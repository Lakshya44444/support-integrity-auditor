# CPU-only PyTorch wheels — far smaller than the default CUDA build,
# which is what makes cloud installs succeed (and faster).
--extra-index-url https://download.pytorch.org/whl/cpu

torch==2.6.0+cpu

# Model-critical pins — the DeBERTa-v3 checkpoint requires transformers 4.46.x
# (5.x computes DeBERTa-v3 differently and breaks inference).
# The fast tokenizer loads from tokenizer.json, so sentencepiece is NOT needed.
transformers==4.46.3
huggingface_hub==0.26.2
tokenizers>=0.20,<0.21

# Scientific stack — ranges so wheels exist on the cloud's Python (3.12/3.13)
numpy>=1.26,<3
pandas>=2.1
scikit-learn>=1.4

# Web app
streamlit>=1.40
plotly>=5.18
